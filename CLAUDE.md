# 掌中古蜀 · 三星堆

新会话先读 `docs/HANDOFF.md`（现状、决策、下一步），写任何文案前对照 `research.md`。

## 不可违反

- **事实与假说分开**：碳十四测年、跨坑拼对、掩埋层序是事实；"那一夜为什么"的四种解释一律是假说，不下定论、不预设。
- **手印必须有出土文物为证**，且要能在手机竖屏的胸前/头侧空间完成。
- **只做网页版**（GitHub Pages），不为小红书小工具容器做适配。
- 非官方作品：README 的声明要保留；`docs/*Brief*.pdf` 已 gitignore，不得提交。
- 改 `mudra.js` 阈值或 `zones` 后，必须重跑 `mudra-test.html` 全绿。

## 验证

- 手势/摄像头相关：本机没有摄像头，**推到线上让 Lyn 用手机测**；内置浏览器面板隐藏时 rAF 不跑、截图陈旧，别据此判断渲染。
- 部署用 `lynlyn0215` 账号；`git push origin main` 即上线。
